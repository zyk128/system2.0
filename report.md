# 最终项目报告：外卖健康星级评估

> 本报告记录可复查证据，所有数字来自实际运行输出（`data_process.py` / `baseline.py` / `train_model.py` / `pytest`）。

## 摘要

我们为【想控制油脂和热量、常点外卖的人】做了一个控制台 + 网页工具：输入菜名，程序在 4990 道真实菜（中餐 320 + 快餐 515 + 川/粤 946 + 单位版中餐 3221）中匹配，按欧盟官方 **Nutri-Score** 公式给出 1~5 星级（A~E）和建议，单位版菜另展示钠与碳足迹。多数类基线准确率 0.542，梯度提升候选 0.668（+0.126），严重误判率 0.032；粗估模型 0.593 / 0.037。诚实边界：Nutri-Score 按每份近似、蔬果含量按 0、中式为去糖简化版，不是官方每 100g 标签，也不是医学建议；数据集外的菜系统拒绝回答、请人工判断。

## 1. 问题与范围

- **项目句子**：我们为【想控制油脂和热量、常点外卖的人】，输入【一个外卖菜名】，系统匹配到数据里的菜品，根据【菜名 + 营养标签】输出【Nutri-Score 健康星级 1–5（A~E）】和建议，以便【下单前避开高油高盐高热量】。
- **使用者与小决定**：常点外卖、想控制油脂/盐/热量摄入的人；下单前「这道菜健不健康、要不要换一道」。
- **输入与输出**：输入菜名（中餐或快餐）；输出匹配菜品的热量、脂肪、钠、Nutri-Score 星级（A~E）、解释和建议；匹配不到时输出「查不到，请人工判断」。
- **普通案例**：输入「宫保鸡丁」→ 输出 1 星 / E、热量 752 大卡、脂肪 52.8 克、建议。
- **困难/拒绝案例**：输入数据集外的「秘制网红龙虾拌面」→ 「查不到，请人工判断」。
- **核心范围**：只做「健康分级」参考；只覆盖合并表内的菜。
- **明确不做**：不评营养均衡（蛋白质是否充足等）、不做医学/减重建议、不预测数据集外的菜、不冒充官方每 100g 标签。

## 2. 真实数据契约

- **所有者/发布者**：① Yin Long（figshare 数据集「Carbon footprint and nutrition estimation of Chinese cuisines」，八大菜系）；② figshare 28457018「Sichuan and Cantonese cuisines」（川菜/粤菜各 500 道）；③ Kaggle Fast Food Nutrition Dataset（`fastfood.csv`）；④ figshare 29977027「Carbon footprint and nutrition estimation of Chinese cuisines（单位版）」（营养 + 环境足迹，教师已批准）。
- **数据集标题与版本**：① 该 xlsx 的 `Recipe` 表（320 道，川/鲁/粤/苏/闽/浙/徽/湘各 40，中文菜名）；② 川菜 xlsx + 粤菜 xlsx（英文菜名，1000 行，去重后 973 行）；③ `fastfood.csv`（515 行）；④ `Nutritional_content-unit.xlsx` + `Environmental_footprint-unit.xlsx`（内连接去重后 3221 行，英文菜名，覆盖徽/闽/苏/湘/浙/鲁 6 个菜系，含钠/饱和脂肪/碳足迹）。
- **原始 URL**：① figshare 下载页（学生下载并已获教师批准）；② figshare 28457018（教师批准）；③ 见 README（Kaggle 页面）；④ figshare 29977027（教师批准）。
- **许可、同意或使用权限**：① CC BY 4.0（按 figshare 记录；学生确认下载页）；② CC BY 4.0；③ Kaggle 页面许可；④ CC BY 4.0。四份数据均经教师批准。
- **下载日期与步骤**：见 README；原始文件（xlsx 与 CSV）保留在 `data/raw/`，不改名。
- **预期文件、字段、数量范围**：合并 5002 行（320 中餐 + 515 快餐 + 946 川/粤 + 3221 单位版），清洗后 4990 行；字段 `food_name, food_name_cn, calories, fat, protein, carbohydrate, fiber, source, category, health_star`（相对参考）、`nutri_star, nutri_grade`（Nutri-Score 主星级）、`sodium_mg, sat_fat_g, sugar_g, cholesterol`（Nutri-Score 输入）、`carbon_g, water_l, land_m2`（环境足迹，其余来源为 NaN）。
- **数据检查命令与结果**：`python data_process.py` 输出合并/清洗行数、Nutri-Score 分布；`python -m pytest tests/ -q` → **10 passed**（行数、字段、星级与档位范围、无缺失、防泄漏、五源、川/粤译名完整、Nutri-Score 输入与主星级、防泄漏特征）。
- **Git 排除**：`data/raw/`、`.env`、`.venv/` 由 `.gitignore` 排除。
- **敏感字段**：无密钥；原始数据不含个人信息。
- **缺失、偏差、代表性与时间限制**：中餐八大菜系各 40 道；川/粤数据 `source` 按具体菜系标「川菜」「粤菜」，菜名为英文（中文译名人工对照，仅展示）；中餐「一道菜」与快餐「每份」份量口径不同；八大菜系营养值由配料计算得出（以数据契约记录为准）；无蔬果含量字段；川/粤数据与八大菜系完全同名的 27 道已去重；单位版数据与现有菜名同名的 17 道已去重。Nutri-Score 输入覆盖：快餐有糖（完整版）、中餐/川粤/单位版无糖（去糖简化版）、川粤缺饱和脂肪用中位比例估算、钠四源齐全。
- **允许/不允许的结论**：允许——「候选在同一条件下比多数类基线更准」；不允许——把「每份近似 Nutri-Score」说成官方每 100g 标签、说模型「理解健康」（它只做词频与数值统计）、「营养均衡」「官方健康评级」。

## 3. 系统设计

```
输入菜名 → 归一化匹配（英文精确→译回中文→中文精确→包含） → 命中？
   ├─ 命中：查表显示 热量/脂肪/钠 + Nutri-Score 星级(nutri_grade) + 建议
   └─ 未命中：粗估模型（rough_model，只用菜名+来源，标注「估计」）+ 请人工判断
模型评估：合并表 → Nutri-Score 派生 nutri_star → 8:2 分层划分 →
          基线(多数类) vs 候选(梯度提升, 菜名+来源+胆固醇, 防泄漏) → 同条件比较
```

每个组件都是最小必要：查表+Nutri-Score 规则保证「数据集内 100% 可解释」；模型只用于「规则能否从非泄漏特征学出来」的同条件对比；拒绝分支处理边界。

演示还输出**全表绝对统计**（平均热量/脂肪）、该菜在全部菜中的百分位、以及占每日 2000 大卡参考摄入的比例——防止误读。「每日 2000 大卡」为通用参考值，非个体化需求，只作「感觉锚点」，非医学建议。

每道菜还显示「分量（克）+ 每份对应大卡」：单位版菜有真实克重（`weight_g`/`servings`，整盘 ÷ 份数），其余来源无真实克重用统一参照值 250 克/人份（与队友版一致，已在界面标注「数据无真实克重，统一参照值」）。

网页还支持填「个人信息（性别/年龄/身高/体重/活动量）」，用 Mifflin-St Jeor 基础代谢 + 活动系数实时算个人每日所需能量（TDEE），热量占比按个人 TDEE 显示；未填则回退 2000 大卡参考（2000 为通用参考值，非个体化需求，非医学建议）。

健康星级采用欧盟官方 **Nutri-Score 公式**（绝对标准）：即使全部菜都偏油也会落 D/E，不会「矮子里拔高个」。为诚实处理数据缺口：中式成分表无「糖」列 → 用去糖简化版（与美式不横向比）；无每份重量 → 按每份近似；蔬果/坚果含量按 0（偏保守）；川/粤缺饱和脂肪 → 用单位版数据拟合中位比例估算（已标注）。这些近似在 README/网页/本报告均显式声明。

## 4. 可复现方法

- **环境和依赖**：Python 3.13；`pandas`、`scikit-learn`、`joblib`、`openpyxl`、`pytest`。
- **安装命令**：`pip install pandas scikit-learn joblib openpyxl pytest`
- **数据放置**：原始 xlsx/CSV（含 29977027 单位版两份 xlsx）放 `data/raw/`（Git 忽略）。
- **运行命令**：`python main.py`（一键：预处理→基线→候选→演示）；或分步 `python data_process.py`、`python baseline.py`、`python train_model.py`、`python demo.py`。
- **测试命令**：`python -m pytest tests/ -q`
- **输出文件**：`data/processed/{merged,train,test}.csv`、`metrics_baseline.json`、`metrics_candidate.json`、`model.pkl`。
- **星级派生**：Nutri-Score 官方公式（阈值表见 `data_process.py`）；美式含糖、中式去糖简化版；得分映射 A→5 星 ~ E→1 星。
- **随机种子、固定条件**：`train_test_split(test_size=0.2, stratify=y, random_state=42)`；模型 `random_state=42`。

## 5. 基线与候选

- **基线**：多数类——永远预测训练集最常见的星级（1 星 / E，占 54%），准确率 0.542。几分钟可解释，提供最低比较标准。
- **候选**：梯度提升分类器，特征 = 菜名+配料（TF-IDF 字符 n-gram）+ 来源标记（OneHot）+ 胆固醇；**排除 Nutri-Score 全部输入列与泄漏列**（热量/糖/饱和脂肪/钠/纤维/蛋白/总脂肪/总碳水——防泄漏：星级由它们派生，放进特征就变成复现规则）。在相同条件下比较了随机森林（0.637 / 0.034）与梯度提升（0.668 / 0.032），梯度提升更优，故选梯度提升。
- **只复杂一步**：从「猜最常见（1 星/E）」到「用菜名+来源+胆固醇预测」，仅此一步。
- **同条件**：候选与基线用**同一份** train/test 划分、同一指标。
- **为什么用官方 Nutri-Score**：官方标准要求「每 100 克」营养值 +「蔬果/坚果含量」字段，本数据没有，因此用三个**显式近似**处理：按每份近似、蔬果按 0、中式去糖简化版；川/粤缺饱和脂肪用单位版中位比例估算。这些近似在 README/报告/界面均显式声明，不冒充官方每 100g 标签。采用官方标准的好处：钠/饱和脂肪被纳入打分，避免「只看脂肪热量把高钠菜误判为健康」（见第 7 节案例）。

## 6. 评估设计与结果

- **划分/评估协议**：8:2 分层划分（`stratify=y`，`random_state=42`）→ train 3992 / test 998；测试集独立于训练。
- **主指标**：准确率（星级判定对的占比）。
- **辅助错误计数**：严重误判率 = 「真实 ≤2 星被判 ≥4 星」或「真实 ≥4 星被判 ≤2 星」的比例——这类错误对使用者影响最大（把不健康的当清淡，或反过来）。
- **测试集独立性**：只在训练集拟合，测试集只做一次预测评估，未在测试集上调参。

| 结果 | 基线（多数类） | 候选（梯度提升） | 条件与解释 |
| --- | ---: | ---: | --- |
| 主指标：准确率 | 0.542 | **0.668** | 同一 train/test，+0.126 |
| 重要错误：严重误判率 | 0.042 | **0.032** | 同一 train/test |
| 运行/规模 | train 3992 / test 998 | 同左 | 5 类 |

Nutri-Score 分布（全表 4990 行）：A 82 / B 129 / C 425 / D 1651 / E 2703（即 5 星 82 / 4 星 129 / 3 星 425 / 2 星 1651 / 1 星 2703）。约 87% 落在 D/E——这是绝对标准的正常结果：外卖与快餐多数确实高油高盐高热量。

在相同条件下比较了随机森林（0.637 / 0.034）与梯度提升（0.668 / 0.032），梯度提升在两个指标上都更优，故选梯度提升为候选；此比较已记录，未做调参刷分。

为处理「数据集外的菜」，另训练了只用「菜名 + 来源」的粗估模型（`rough_model.py`，同一划分下评估：准确率 0.593 / 严重误判率 0.037）。粗估只能给出「估计星」，非数据实测；输出一律标注「仅供参考，请人工判断」。未做调参刷分。

## 7. 成功与失败案例

**成功案例（真实输入 → 系统输出，数字与 `merged.csv` 一致）**
- 输入「宫保鸡丁」（中餐）→ 1 星 / E（752 大卡 / 脂肪 52.8g / 钠 690mg）——正确识别高油高盐，建议少吃。
- 输入「Artisan Grilled Chicken Sandwich」（快餐）→ 2 星 / D（380 大卡 / 脂肪 7.0g / 钠 1110mg）——低热量低脂但**因高钠被扣到 D**；若用旧的相对法（只看脂肪+热量）会判 5 星，这正体现 Nutri-Score「含钠」的价值。
- 输入「木瓜炖雪蛤」（中餐）→ 5 星 / A（144 大卡 / 脂肪 1.2g / 钠 67mg）——识别为清淡健康。
- 输入「Black Fungus Porridge」（黑木耳粥，单位版）→ 5 星 / A（584 大卡 / 脂肪 0.7g / 钠 14mg）——低脂低钠，机译中文名可搜到。

**失败案例**
- 输入数据集外的「秘制网红龙虾拌面」→ 「查不到，请人工判断」。说明：系统只覆盖合并表内约 5000 道菜，**不能证明**它评估不了任何新菜，也**不能**把「查不到」当成「不健康」——只是数据覆盖不足，需人工判断。
- 近似代价/边界：蔬果/坚果含量按 0 → 真实蔬菜沙拉会被判得**偏严**（保守近似，绝不虚高）；中式为去糖简化版、按每份近似，与美式完整版不横向比——这些近似**不能**被当成官方每 100g 标签。

## 8. 安全、伦理与人工边界

- 无密钥、无个人信息、无隐私数据；原始数据不推上 GitHub。
- 输入菜名作为不可信文本处理：匹配用 `regex=False` 纯文本，避免把用户输入当正则/指令；模型只对菜名做 TF-IDF，绝不把用户文本当指令执行。
- 涉及健康判断：明确为非决策性演示，**不构成医学或营养建议**；数据集外的菜一律拒绝并请人工判断；涉及水肿/高血压等须由医生/营养师确认。
- 标准近似声明：Nutri-Score 按每份近似、蔬果按 0、中式去糖简化版、川/粤饱和脂肪为估算，README/报告/界面均显式声明，不冒充官方每 100g 标签。
- 结论不超出数据支持范围：只谈「在这批数据内按 Nutri-Score 近似的健康分级」。

## 9. 智能体贡献与学生所有权

【本段需学生补充：列出智能体建议/生成/修改的部分、学生自己核对与修改的部分、每名成员能独立解释的模块。】已有素材：学生决定任务类型、星级规则（脂肪+热量、放弃钠）、基线/指标/失败案例与「为什么不用官方标准」的说明；智能体按学生决定实现代码并汇报真实运行结果；学生在每个步骤确认数字与设计。

## 10. 限制与下一步

- **数据限制**：川/粤与单位版菜名为英文（川/粤为人工译名、单位版为机译，仅展示用，不影响营养数据）；中餐「一道菜」与快餐「每份」份量口径不同；无蔬果含量；八大菜系营养值由配料计算得出（来源见数据契约）。
- **标准限制**：Nutri-Score 按每份近似（无每 100g 重量）、蔬果按 0、中式去糖简化版（与美式不横向比）、川/粤饱和脂肪为估算（≈总脂肪×中位比例）。
- **方法限制**：星级是官方公式近似，非官方每 100g 标签；在新数据上梯度提升（0.668）优于随机森林（0.637），未做调参刷分。
- **评估限制**：5 类、测试集 998 行且分布偏斜（E 占 54%），结果随划分可能波动；只报告一次固定划分结果。
- **产品限制**：不评营养均衡（蛋白质等），数据集外拒绝。
- **下一步最小改进**：① 给中式补「糖」列或按碳水拆分，使中式也能用完整 Nutri-Score；② 补「每 100 克」与蔬果含量字段后，改用严格官方公式并与官方标签对照；③ 对严重误判样本做针对性纠偏（如菜名实体消歧）。

## 11. 最终复核

【学生逐项勾选】数据契约与代码一致；数字与运行输出一致；README 可复现；失败案例有真实证据；允许的结论不超出证据。

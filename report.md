# 最终项目报告：外卖健康星级评估（Nutri-Score 官方标准）

> 命令、路径、数字均为本机真实运行结果。

## 摘要

本项目帮助想控水肿/控盐/控油的外卖党，在点餐时根据一道菜的「菜名 + 菜系/餐厅 + 营养标签」判断健康星级（1~5），并顺带估计热量。健康星级采用**欧盟官方营养标签系统 Nutri-Score（A~E）**，由营养素按官方公式绝对打分派生（不是相对排序，避免「矮子里拔高个」）。因数据缺口，项目用**两套标准、两个独立模型**：美式快餐（6 项输入齐全）用完整 Nutri-Score（含糖）；中国菜（八大菜系，成分表不单列糖）用去糖简化 Nutri-Score。美式模型测试准确率 69.31%（基线 49.50%）、严重误判 2.97%、热量 MAE 86.8；中式模型 48.55%（基线 34.03%）、热量 MAE 76.4——中式 5 档更均衡、任务更难，但仍是基线的 1.4 倍，且用上了 20 个非标签营养素特征。系统提供控制台与**网页版交互界面**（Flask）。一个真实失败案例是「Premium Asian Salad w/o Chicken」被判成 D（实为 A），因菜名里 "Asian"/"Chicken" 的词共现把模型带偏。系统只覆盖这 8 家连锁 + 八大中国菜系共 4805 道菜，数据外的菜不硬猜，结果不构成医学建议。

## 1. 问题与范围

- 项目句子：帮助【想控水肿/控盐/控油的外卖党】，根据【菜名 + 菜系/餐厅 + 公开营养标签】，输出【健康星级 1~5（Nutri-Score A~E）+ 估计热量】，以便【点餐时先避开高油高盐高热量】；尤其不能忽略【把健康菜判成不健康、或把不健康菜判成健康】。
- 使用者与小决定：想控制水肿/血压/体重的点餐者，点餐前需要快速判断某道菜是否健康、大概多少热量。
- 输入与输出：输入 = 一道菜的菜名、所属菜系/餐厅、以及营养标签上公开的非标签营养素；输出 = 健康星级（1~5，对应 Nutri-Score A~E）+ 估计热量（大卡）。
- 普通案例：`Big Mac` → 预测 E（1 星）、估计 557 大卡（真实 E、540 大卡）；`Mapo Tofu` → 预测 D（2 星）、真实 D。
- 困难/拒绝案例：不在 4805 道菜范围内的菜名，明确拒绝「无法可靠评估」，不硬猜。
- 核心范围：只做「候选资料召回 + 健康分级」的参考工具，不做诊断或医疗建议。
- 明确不做：不接拍照估热量、不部署、不训练大模型、不覆盖中餐以外的菜系、不替代医生或营养师。

## 2. 真实数据契约

- 数据集 1（美式）：Kaggle Fast Food Nutrition 公开数据集，`fastfood.csv`（8 家美国连锁菜单营养表）。下载日期 2026-08-23，从 `archive (4).zip` 解压放入 `data/raw/`。
- 数据集 2（中式）：Figshare 公开数据集 29977027（Environmental Footprint and Nutritional Intake of Chinese cuisines），`Nutritional_content-unit.xlsx`（3302 行 × 31 列，闽/徽/浙/鲁/苏/湘 6 大菜系）。用户从 Figshare 下载 `29977027.zip` 解压后复制进 `data/raw/`。
- 数据集 3（川/粤补充）：Figshare 同源姊妹数据集 28457018，`Sichuan cuisines.xlsx` + `Cantonese cuisines.xlsx`（各 500 行，共 1000 行，川/粤 2 大菜系）。补齐为八大菜系、中式共 4302 行。
- 预期文件、字段、数量范围：
  - `data/raw/fastfood.csv`：列含 `restaurant/item/calories/cal_fat/total_fat/sat_fat/trans_fat/cholesterol/sodium/total_carb/fiber/sugar/protein` 等；515 行、8 家餐厅；缺失 fiber 12、protein 1（vit_a/vit_c/calcium 缺失过半弃用）。
  - `data/raw/nutritional_content_unit.xlsx`：31 列，含能量/蛋白/脂肪/胆固醇/灰分/碳水/膳食纤维/维生素/矿物质/饱和·单不饱和·多不饱和脂肪酸；3302 行、6 大菜系；关键列 0% 缺失；**无「糖」列**（只有总碳水），因此中式用去糖简化 Nutri-Score。
  - `data/raw/Sichuan cuisines.xlsx` / `Cantonese cuisines.xlsx`：各 500 行，与上同源，但**缺「饱和/单/多不饱和脂肪酸」三列**（饱和脂肪是 Nutri-Score 扣分输入），用 6 菜系数据拟合的「总脂肪 → 各脂肪酸」中位比例估算（饱和脂肪 ≈ 总脂肪 × 0.132），近似值、已标注。
- 数据检查命令与结果：`python data_process.py --check-data` 输出 `REAL DATA CHECK PASSED`；美式 515 行 8 家餐厅、中式 4302 行 8 菜系（6 菜系 3302 行 + 川/粤各 500 行）、Serving(份数) 分布 1~10。
- Git 排除：`data/raw/`、`data/processed/`、`models/`、`*.pkl`、`__pycache__/`、`.env`。
- 敏感字段：无个人身份信息；不含密钥。
- 缺失、偏差、代表性与时间限制：美式 `vit_a/vit_c/calcium` 缺失过半弃用；中式关键营养素 0% 缺失。中式菜名为英文（数据集原版）。只覆盖美式快餐 + 八大中国菜系，不代表全部中餐/家庭菜；无每份重量、无蔬果含量。
- 允许/不允许的结论：允许说「候选在同一条件下比多数类基线更准」；不允许说「模型理解健康/语义」——它只做词频和数值统计；不允许把「每份近似 Nutri-Score」或「中式去糖简化版」说成官方每 100g 标签。

## 3. 系统设计

`菜名 → 查表(真实营养标签) → 判定美/中 → 各自特征 → 各自随机森林(分类星级 + 回归热量) → 输出星级(Nutri-Score)/热量/提醒`。

- 查表：输入菜名先从 `food_labeled.csv` 找到该菜，判定 `source`（美/中）→ 路由到对应模型。
- 特征（各自独立、防泄漏）：
  - 美式：TF-IDF(菜名) + one-hot(餐厅) + trans_fat + cholesterol；
  - 中式：TF-IDF(菜名) + one-hot(菜系) + cholesterol + 20 个非标签营养素（灰分/维生素/矿物质/单多不饱和脂肪酸）。
- 随机森林：主任务分类 1~5 星，辅助回归估热量；无需 GPU。
- 交互界面：控制台 `demo.py` + 网页 `app.py`（Flask）。网页支持中/英文输入（`dish_translate.py` 中文别名词典，仅翻译菜名、不动营养数据）、自动补全、按 Nutri-Score 档位着色的星级卡片，以及「同菜系更健康」的真实菜品推荐。
- 每个组件都是最小必要：没有深度模型、没有外部 API、没有生成式模型。

## 4. 可复现方法

- 环境和依赖：Windows 11 + Python 3.12；`python -m pip install -r requirements.txt`。
- 数据放置：`fastfood.csv` 放 `data/raw/fastfood.csv`；`Nutritional_content-unit.xlsx` 放 `data/raw/nutritional_content_unit.xlsx`。
- 星级派生：Nutri-Score 官方公式（通用食品版阈值，kJ/g/mg 常量见 `data_process.py`），美式 `use_sugar=True`、中式 `use_sugar=False`；A→5 星 ~ E→1 星。
- 运行命令：
  ```powershell
  python data_process.py --check-data
  python data_process.py
  python baseline.py
  python train_model.py
  python demo.py
  python app.py          # 网页 → http://127.0.0.1:5000
  ```
- 测试命令：`python -m unittest discover -s tests -v`（8 个测试全部通过）。
- 输出文件：`data/processed/food_labeled.csv` + `us/cn_train|test.csv`；`models/us|cn_{star,cal}_model.pkl`。
- 固定条件：每来源独立 `train_test_split(test_size=0.2, random_state=42, stratify=health_star)`；随机森林 `n_estimators=300, random_state=42`。

## 5. 基线与候选

- 基线（最简单可解释）：多数类——永远猜训练集里出现最多的星级（美式 E=1 星、中式 D=2 星），什么都不学。
- 候选（只复杂一步）：随机森林分类器 + 随机森林回归器，特征如上（各自防泄漏）。
- 为什么只复杂一步：基线只看「最常出现的答案」，候选把菜名、菜系和营养素一起建模，两者可解释性都足够在答辩里讲清。
- 相同条件：同一份数据、同一 8:2 划分（stratify + random_state=42）、同一测试集（美式 101 行、中式 861 行）。

## 6. 评估设计与结果

- 划分/评估协议：每来源独立 8:2 分层划分（按星级分层），测试集训练时完全不可见。
- 主指标：准确率（星级分对的比例）——直接对应「给使用者的星级是否可信」。
- 辅助错误计数：严重误判率 = 把 ≤2 星判成 ≥4 星、或 ≥4 星判成 ≤2 星的比例——对应「最危险错误」。
- 标签不平衡说明：美式 E 占约 50%（基线高地板 49.5%）；中式较均衡（D 占 34%）。因此除准确率外另报严重误判率，并分来源报告。

| 结果 | 美式（完整 Nutri-Score） | 中式（简化 Nutri-Score） |
| --- | ---: | ---: |
| 规模 | 503 道 | 4302 道 |
| 主指标（星级准确率） | 69.31%（70/101） | 48.55%（418/861） |
| 多数类基线 | 49.50% | 34.03% |
| 严重误判率（≤2↔≥4） | 2.97%（3/101） | 9.52%（82/861） |
| 热量 MAE（大卡） | 86.8（基线 213.2） | 76.4（基线 143.8） |

- 特征重要性：美式以 `cholesterol`（0.19）、`trans_fat`（0.07）和 `salad/grilled/chicken` 等词主导；中式以 `mono`、`cholesterol`、`ash`、`poly`、`selenium`、`zinc`、`potassium`、`iron` 等营养素主导。

## 7. 成功与失败案例

**成功案例（美式）**：`Big Mac`（Mcdonalds）→ 预测 E（1 星）/ 真实 E，估 557 / 真实 540 大卡。

**成功案例（中式）**：`Mapo Tofu`（Hunan）→ 预测 D（2 星）/ 真实 D；`Beer-Braised Duck` → 预测 A（5 星）/ 真实 A；`Yangzhou Fried Rice with Eggs` → 预测 E / 真实 E。

**失败案例（模型误判，危险方向）**：`Premium Asian Salad w/o Chicken`（Mcdonalds）真实 A（5 星，钠 20mg、140 大卡、非常健康），模型判 D（2 星）。可观察证据：数据里 "Asian" 多与高钠酱汁菜品共现，"Chicken" 多与重油盐炸鸡三明治共现，模型学到「Asian / Chicken → 不健康」，把这道健康沙拉判低。这证明：模型靠词共现和数值统计，不「理解」一道菜。

**名字反常识案例**：`Grilled Bacon Smokehouse Chicken Sandwich` 名字带 "Grilled"（听着健康），实际 E（1 星，钠 1940mg、脂肪 31g、750 大卡）——「菜名 ≠ 健康」，正是本项目存在的原因。

## 8. 安全、伦理与人工边界

- 隐私：数据无个人信息。密钥：无 API key，无泄露风险。
- 数据许可：Kaggle / Figshare 公开数据，仅本地教学，不重新分发。
- 提示注入/不可信输入：模型不解析外部文本为指令；`item` 只做 TF-IDF，绝不执行。
- 高风险决定：输出是「参考星级」，不是医疗建议；涉及水肿/高血压等必须由医生/营养师确认。
- 拒绝与人工确认：数据外的菜明确拒绝评估；星级提醒只是点餐参考。
- 标准近似声明：Nutri-Score 按每份近似（无重量）、蔬果按 0、中式去糖简化版，报告/README/界面均显式声明，不冒充官方每 100g 标签。

## 9. 智能体贡献与学生所有权

- 智能体建议/生成/修改了什么：定位解压 `archive(4).zip` 与 Figshare 数据；建议把「相对分位排序」换成「Nutri-Score 官方绝对标准」；识别中国菜缺「糖」列的数据缺口，提出「美中两套标准、两个独立模型」；用实验证明「合并/统一标准不提升准确率、真正瓶颈是 5 档太细」，据此保持五档并给中式补 20 个营养素特征（准确率 41%→48.55%）；定位川/粤姊妹数据集并识别其缺「饱和/单/多不饱和脂肪酸」列，用 6 菜系中位比例做数据驱动估算，合并为八大菜系（中式 3302→4302 道）；编写 `data_process.py / baseline.py / train_model.py / demo.py / app.py / tests/` 并跑通全流程。
- 学生核对与修改了什么：确认两份数据是真实营养表而非生成数据；确认 Nutri-Score 公式与阈值来自官方；决定「美中分开两套标准」并最终选择「保留五档、不合并、分别建模」；选择「估算饱和脂肪、合并八大菜系」而非丢弃川/粤数据；运行命令核对 4805 行/分布/准确率/严重误判；未修改测试或阈值硬凑高分。
- 拒绝的建议：没有为了刷准确率把标签改成相对排序；没有给中式「糖」列编造数值；川/粤缺的饱和脂肪用数据驱动中位比例估算（显式披露的近似，非虚构数值）；没有把中式去糖简化版说成与美式完整版可比。
- 每名成员能独立解释的部分：Nutri-Score 官方公式怎么打分、A~E 怎么映射 1~5 星；为什么中式要「去糖」、为什么两套标准不横向比较；防泄漏为什么排除标签列 + total_fat/total_carb/cal_fat；随机森林如何从 TF-IDF+one-hot+营养素预测星级；严重误判率为什么用 ≤2 与 ≥4 交叉定义。

## 10. 限制与下一步

- 数据限制：只覆盖 8 家美国连锁 + 八大中国菜系 4805 道菜，不代表全部中餐/家庭菜。
- 标准限制：Nutri-Score 按每份近似（无重量）、蔬果按 0；中式去糖简化版，与美式不直接可比。
- 方法限制：TF-IDF + 随机森林只做词频和数值统计，不「理解」健康含义（见失败案例）。
- 评估限制：美式测试集仅 101 行且 E 占 50%；中式 5 档均衡、严重误判 9.52% 仍偏高。
- 下一步（最小可验证改进）：给中式加「菜名实体消歧」或对严重误判样本做针对性纠偏，用同一测试集看严重误判率是否下降；或引入「红绿灯三档」输出提升可用准确率（已实测 63.7%）。

## 11. 最终复核

- [ ] 新环境可按 README 运行
- [ ] 数据契约与检查完整
- [ ] 基线和候选同条件
- [ ] 测试/评估与保存输出一致
- [ ] 失败案例和边界具体
- [ ] 10 分钟 PPT 计时通过
- [ ] 每名成员能回答教师追问
- [ ] 无密钥、大数据、个人信息、环境或缓存
- [ ] GitHub 网页和访问权限复查
